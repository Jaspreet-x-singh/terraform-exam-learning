# A Terraform workspace is a feature that isolates multiple state files within the same working directory, allowing you to deploy separate instances of the same infrastructure configuration. 

# commands
# You can manage CLI workspaces with these subcommands: 
# terraform workspace list: Lists all existing workspaces.
# terraform workspace new <name>: Creates a new workspace and switches to it.
# terraform workspace select <name>: Switches to an existing workspace.
# terraform workspace show: Displays the name of the currently active workspace.
# terraform workspace delete <name>: Deletes an inactive